# セッションハンドオフ（2026-05-07 02:00）

## 再開プロンプト

```
docs/design/unattended-completion.md と docs/design/mcp-reconnect.md を読んでください。
前回セッションの続きです。以下の状態から再開します。
```

## 現在の状態

### デプロイ済み（本番で動作中）

deploy15 まで完了。以下が全て本番に反映済み:
- MCP 再接続（`mcp_reconnect.py`）
- タイムアウト設定（`read_timeout=300`, `mcp timeout=360`, `_TOOL_TIMEOUT=360`）
- adaptive thinking + `max_tokens=128000`（Opus 4.7/4.6）
- エラー表示（`streaming.py` + `strandsParser.js`）
- 観測性（SIGTERM, ping, version, session_start, compose ログ）
- LoopGuard 改善（fingerprint エラーのみ、上限引き上げ）
- 401 検知 → `auth_expired` イベント

### リモート（GitHub main）に未反映

ローカルの全変更が未 push。PR を作成する必要あり。
変更ファイル一覧は `docs/design/unattended-completion.md` の「変更ファイル一覧」セクション参照。

### PR #118（fix/agent-error-display）

マージ済み。レビューで指摘された WebUI エラー分類のリファクタリングは別 issue で追跡。

### PR #119（feat/mcp-auto-reconnect）

取り下げ済み（バグがあったため）。リファクタ後の新コードで再作成する必要あり。

## 未対応の残課題

### 高優先度

1. **会話履歴の画像間引き** — Too much media を根本的に防止
2. **WebUI 自動リトライ** — コールドスタートタイムアウト時に `agentCoreService.js` で自動リトライ

### 中優先度

3. **WebUI `auth_expired` 表示** — 「メッセージを送り直してください」と表示
4. **WebUI `isHardStopped` 改善** — SSE イベント欠落時に履歴 API から結果を再取得

### 低優先度

5. **Composer ハードタイムアウト** — タイムバジェット 2 倍超で強制停止
6. **テンプレート名エイリアス** — `aws-brand-black` → `aws-brand-dark`

## 設計ドキュメント一覧

| ファイル | 内容 |
|---|---|
| `docs/design/unattended-completion.md` | 全体像: 事象・対処・設定値の根拠・変更ファイル一覧 |
| `docs/design/mcp-reconnect.md` | MCP 再接続の設計: クラス仕様・フロー・テスト仕様 |
| `docs/changelog/2026-05-05-agent-hang-investigation.md` | 調査ログ: CloudWatch/CloudTrail コマンド集・再発時プロンプト |

## 次にやるべきこと

1. **PR 作成** — ローカルの変更を新ブランチに push して PR を作成
2. **画像間引き実装** — `agent.messages` から古いプレビュー画像を間引く
3. **WebUI 自動リトライ** — `agentCoreService.js` にタイムアウト時リトライ追加

## AWS リソース

- Account: 867694312594, Region: us-east-1
- Agent Runtime: `sdpm_agent-20nCH0F8Em`
- MCP Server Runtime: `sdpm-rICI8O3LbR`
- Agent ログ: `/aws/bedrock-agentcore/runtimes/sdpm_agent-20nCH0F8Em-DEFAULT`
- MCP ログ: `/aws/bedrock-agentcore/runtimes/sdpm-rICI8O3LbR-DEFAULT`
- CloudFront: `https://dpog5vhysrytq.cloudfront.net`
